apps:
  miot-bridge:
    udpIngress:
      entrypoint: udp-miot-sbx
    image:
      tag: 0.3.1
    replicas: 1
    templates:
      config:
        content: |
          {
              "serviceName": "{{ APPLICATION }}",
              "server": {
                  "httpPort": "{{ CONTAINER_PORT }}"
              },
              "publicURL": "{{ VAR_PROTOCOL }}://{{ VAR_SUBDOMAIN }}.{{ COMPONENT }}.{{ VAR_PUBLIC_DOMAIN }}/{{ APPLICATION_GROUP }}/{{ APPLICATION }}",
              "cachePath": "./cache",
              "udp": {
                  "enabled": true,
                  "port": {{ CONTAINER_UDP_PORT }}
              },
              "mqtt": {
                  "enabled": true,
                  "url": "{{ VAR_MQTT_URL }}",
                  "clientId": "miot-bridge-sandbox",
                  "username": "{{ SECRET_MQTT_MIOT_BRIDGE_USERNAME }}",
                  "password": "{{ SECRET_MQTT_MIOT_BRIDGE_PASSWORD }}",
                  "topicPrefix": "{{ APPLICATION_GROUP }}/{{ NAMESPACE }}/"
              },
              "polling": {
                  "enabled": true,
                  "intervalMs": 5000,
                  "dispatchOnChange": true,
                  "maxErrorCount": 3,
                  "errorSkipCycles": 20
              },
              "notifications": {
                  "udp": {
                      "enabled": true,
                      "address": "192.168.1.140:50450"
                  },
                  "http": {
                      "enabled": false,
                      "address": ""
                  },
                  "mqtt": {
                      "enabled": false
                  }
              }
          }
