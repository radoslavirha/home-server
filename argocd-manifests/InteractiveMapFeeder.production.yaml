apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-iot-interactive-map-feeder-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/radoslavirha/home-server.git
    targetRevision: HEAD
    path: helm-charts/iot-applications
    helm:
      releaseName: api-iot-interactive-map-feeder
      valueFiles:
        - interactive-map-feeder.yaml
        - environments/production/interactive-map-feeder.yaml
        - environments/production/variables.yaml
  destination: 
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      enabled: true
      selfHeal: true
      prune: true